# CS2 Demo Desk 测试报告

## 1.2.8 启动与下载自动化（2026-09-25）

- 76 项基础测试通过；新增来源与日期过滤、不重复下载、失败保留、暂停和解压等待、自动退出倒计时与取消，以及便携版启动路径测试。
- Electron 端到端测试通过：受控来源获取 → 自动下载新 Demo → 跳过已完成记录 → 修改三项设置 → 取消退出 → 重启保留设置 → 无新任务时自动退出。
- 实际 Windows 启动项 API 的写入、读取与移除通过，使用唯一临时测试项，测试后已移除。未执行真实重启/登录测试。
- 自动获取测试使用本机受控服务，不使用真实账户凭证；来源可用性仍取决于 Steam、完美平台及 HLTV。

## 1.2.7 回放按键录入（2026-09-23）

- 71 项基础测试通过。
- 桌面界面实测按键录入、重复键提示、组合键拒绝、Esc 取消、保存 CFG、重启持久化、禁用和恢复默认通过。
- 延续现有自动加载 CFG 流程，本次不改变游戏启动方式。

## 1.2.6 回放按键编辑（2026-09-21）

- 基础测试 71 项通过：包括重复按键、非法指令拒绝、CFG 替换、禁用配置和播放时加载；保留用户 autoexec。
- Electron 界面测试通过：编辑按键、保存 CFG、冲突提示、切换页面保留草稿、增加操作、重启保留设置、禁用和恢复默认。
- 1.2.6 Windows 便携 EXE 实测通过：自解压启动、加载界面、版本检查，以及在打包后的按键页面保存并生成 CFG。
- 本次新增按键尚未逐项进行真实游戏内按键实测；Steam 自动回放沿用 1.2.5 已获用户确认的实现。

## 1.2.5 实机自动回放验证（2026-09-21）

- Steam 日志确认旧版已经将 +playdemo 与外部录像路径传到 CS2，因此没有将“参数发送”误判为播放成功。
- 新版采用游戏目录内短文件名副本和独立 +exec 配置。实际通过 Steam 启动后，CS2 日志在 20:35:17 显示 execing 配置和 Requesting playback，20:35:26 显示 playing demo from，随后进入资源加载。
- 用户随后确认“已经自动播放了”，实机画面与游戏日志一致。
- 基础测试 69 项通过；桌面端到端验证解压→副本与配置生成→Steam 游戏 730 执行配置。
- 测试确认只清理本工具生成的文件，下载原件和用户 autoexec 保留。

## 1.2.4 Steam 启动与自动播放（2026-09-21）

- 基础测试 69 项、桌面回归 16 项通过。
- 打包程序端到端测试通过：点击“解压并播放”→解压真实 ZIP→启动测试用 steam.exe 参数接收器；接收到 `-applaunch 730 +playdemo <解压后路径> +demoui`，中文和空格路径完整保留，无 `-console`，忽略旧 cs2Path 设置。
- 已向真实 Steam 发出启动请求，用户确认出现 Steam 启动确认窗口。游戏内是否成功载入该录像尚未得到确认；不将参数接收器测试当作游戏回放成功。

## 1.2.3 真实账号接口验证（2026-09-19）

- 本机诊断确认自动抓取、账号识别和加密保存均成功。
- 使用同一份本机凭证发起只读请求：旧请求返回 HTTP 200 / code 1033；去除大小写重复的 SteamID 请求头后返回 HTTP 200 / code 0，并获取 2 场比赛。密钥未输出或写入日志。
- 修正后的生产请求再次实测返回成功。新增测试验证 HTTP 标准化后的 SteamID 值保持单值，以及错误码不会暴露服务端返回的敏感内容。
- 同时修正下载请求中的重复请求头；真实 Demo 文件下载仍需单独验证。
- 默认 size=100 的实测请求被接口以 code 102 拒绝，改为最多 20 场后恢复成功；实际 date 字段解析后，2 场比赛均识别为近 7 天。
- 使用已保存凭证的独立测试资料目录，源程序与打包程序均完成真实接口刷新，界面正确显示近 7 天的 2 场比赛；68 项基础测试通过。

## 1.2.2 自动抓取修复（2026-09-19）

- 新增真实 Electron 窗口回归场景：令牌位于混合查询/片段回跳地址、页面立即清除地址、缺少 Steam 登录 Cookie，使用同一会话的本人资料识别账号。
- 比赛接口模拟返回 503 时，仍加密保存凭证；界面显示获取比赛失败，重启可以恢复凭证，清除有效。
- 登录状态诊断仅记录时间、阶段、捕获/识别/保存布尔值，不记录密钥、Cookie、账号 ID 或带参数地址。
- 测试服务验证通过不代表用户的真实登录问题已得到确认解决；真实授权完整流程仍需使用该用户账号验证。

## 1.2.1 完美平台自动登录（2026-09-15）

- 基础测试 60 项通过；其中 7 项新增测试覆盖官方域名限制、回跳令牌、同会话 SteamID、Cookie 自动获取、重复登录、取消时阻止保存、错误脱敏及并发事件。
- 桌面回归测试 16 项通过。
- Windows Electron 和打包程序均通过模拟官方服务的自动登录流程：真实窗口与 Cookie、令牌校验、本机加密、自动刷新、重启恢复、清除凭证；令牌未进入界面状态或明文比赛库。
- 真实网络已确认完美官方登录入口可跳转 Steam；未输入真实账号凭证，完整授权与真实下载尚待用户在本机验证。

## 1.2.0 增量验证（2026-09-15）

- 基础测试 53 项通过，包括完美平台请求签名、模拟比赛接口、凭证加密、CS2 库路径检测，以及中文/空格录像路径和 DemoUI 启动参数。
- Windows 打包程序桌面测试 16 项通过，包括下载、解压、界面播放入口和重启持久化。
- 自动刷新桌面测试 9 项通过。
- 完美平台尚未使用真实账号验证接口可用性或实际下载；需要在软件中配置凭证后测试。
- 播放验证覆盖启动参数和界面，不代表真实 CS2 已成功载入录像。需已安装 CS2、登录 Steam，并先退出正在运行的游戏。
- 未嵌入 CS2 Insight Agent 的后端或 2D 回放插件。

## 1.1 历史验证

测试日期：2026-09-10。环境：Windows 10 x64（10.0.19045），Electron 41.10.7，Node.js 24.16.0。

## 本次新增能力

- 官匹登录后自动获取优先、竞技、搭档记录，默认近 7 天，可刷新或调整 1–365 天。
- Steam 登录凭证使用 Windows safeStorage / DPAPI 在本机加密保存，跨进程恢复，失效需重新登录。
- HLTV 赛事目录自动获取、按赛事分组和筛选，选择下载时自动解析详情页 Demo。
- 日期未知单独标记；分页不完整、登录失效、网站验证与网络失败分别提示，已有数据保留。

## 自动化结果

| 测试组 | 结果 | 内容 |
| --- | --- | --- |
| 基础逻辑、同步引擎、凭证保存、网页解析 | 46 项通过 | 含真实 Electron DOM 和已保存真实 HLTV HTML |
| 原有桌面下载流程回归 | 15 项通过 | 批量下载、重定向、暂停/继续、并发、取消、BZ2/ZIP/RAR/7z 解压、重启记录与剪贴板 |
| 新增自动获取桌面流程 | 9 项通过 | 自动同步、赛事分类、懒解析下载、登录后继续、范围调整、验证提示、实际 Windows 加密、重启恢复和清除凭证 |
| Windows 发布包自动获取回归 | 9 项通过 | 在最终打包程序上重复验证自动获取与凭证恢复 |
| 1.1.0 单文件便携版启动 | 通过 | 自解包、正确版本、中文界面及全新用户资料启动 |

新增自动流程的 9 项验证：

1. 启动自动获取最近 7 天赛事，识别 Steam 需要登录。
2. 对阵和赛事元数据形成分类，赛事筛选真正影响列表。
3. 选中赛事记录后自动查找详情链接，并下载到磁盘。
4. 模拟登录后的页面导航自动继续获取官匹，自动加载更多，以比赛发生日期过滤。
5. 修改界面范围为 30 天后自动获取较早记录。
6. 网站验证不会被绕过，也不会清空已获取记录。
7. 使用真实 Windows safeStorage 加密写盘，文件不包含测试凭证的明文。
8. 关闭并重新启动软件后恢复登录会话和 30 天配置，再次自动获取。
9. 清除登录后删除加密凭证，保留历史比赛和下载。

同时验证了凭证白名单、过期凭证丢弃、不覆盖新的登录状态、清除/保存竞态、缺少加密服务不明文回退，以及三种同步衔接问题：延迟加载的登录后页面、停止后立即刷新、详情验证状态被目录同步覆盖。

原始记录位于 test-results/desktop-report.json、auto-desktop-report.json；发布包回归记录为 auto-packaged-report.json，最终单文件启动检查为 portable-report.json。

## 真实来源检查

最新真实自动获取检查已正确进入以下状态：

- Steam：login_required，跳转到 steamcommunity.com 官方登录页，等待账号本人首次登录。
- HLTV：verification_required，正确识别当前网络的网站验证，提供验证窗口入口。
- 修复了等待全部图片/广告加载导致超时的问题；页面正文准备好后即可识别登录和验证状态。

原始结果：test-results/live-auto-report.json。

已保存的真实 HLTV 比赛 HTML 解析验证通过：BIG vs G2 / FISSURE Playground 3 / 比赛 2397605 / Demo 111176。Steam 真实比赛历史的表格结构和加载更多控件使用公开源码核实，解析测试覆盖这些 DOM 结构。

## 验证边界

桌面自动化使用本地受控比赛网页、独立资料目录及人工传输样本；没有登录用户的真实 Steam 账号，也没有绕过 HLTV 验证。真实个人完整比赛列表和真实赛事包下载仍需账号本人登录及网站允许访问后确认。

微型测试 Demo 用于检查文件传输、解压和保存，不是可播放的实战比赛录像。没有在 CS2 游戏内验证播放；录像过期、未发布与游戏版本兼容性不由此工具决定。

Windows 10 x64 已实测，Windows 11 x64 尚未单独实测。跨进程重试从头下载，不承诺断点续传。自动翻页最多 20 页，达到上限会标记部分结果。

## 参考

[Steam 个人比赛](https://steamcommunity.com/my/gcpd/730/?tab=matchhistorypremier) · [HLTV 真实样例](https://www.hltv.org/matches/2397605/big-vs-g2-fissure-playground-3) · [Electron safeStorage](https://www.electronjs.org/docs/latest/api/safe-storage) · [Electron 下载行为](https://www.electronjs.org/docs/latest/api/download-item/)
