# CS2 Demo Desk 1.2.7

回放 CFG 可选项集中在软件左侧“回放按键”标签页：

- 以中文选择暂停/继续、倍速、播放面板、透视和观察视角等功能。
- 点击“按键录入”，直接按下想绑定的键；也可在下拉列表选择键盘键、鼠标键或滚轮。
- 录入时检查按键冲突，Esc 取消；目前支持单键绑定，不支持组合键。
- 点击“保存并更新 CFG”，下次一键播放自动加载配置。

新绑定会替换游戏中同名按键。关闭加载或取消绑定不会恢复游戏原键位。

沿用 1.2.6 的完美平台获取和 Steam 自动播放流程。本版本未签名。
